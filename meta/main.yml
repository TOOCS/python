galaxy_info:
  author: Florian Kempenich
  description: Install `python` and `python3`, along with `virtualenv` and `virtualenvwrapper` for `python3`.

  license: MIT

  min_ansible_version: 1.2

  platforms:
    - name: Ubuntu
      versions:
        - all
    - name: MacOSX
      versions:
        - all

  galaxy_tags:
    - python
    - python3
    - python2
    - virtualenv
    - virtualenvwrapper

dependencies: []
